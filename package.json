{
  "name": "parapharmacie-me",
  "version": "1.0.0",
  "description": "parapharmacie.me - parapharmacie e-commerce moderne au Maroc",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "node scripts/build.mjs",
    "lint": "node scripts/validate.mjs",
    "generate:images": "node scripts/generate-product-fallbacks.mjs",
    "generate:sitemap": "NODE_NO_WARNINGS=1 node scripts/generate-sitemap.mjs",
    "validate:catalog": "NODE_NO_WARNINGS=1 node scripts/validate-catalog.mjs",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "vite": "^8.0.10"
  }
}
