rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /products/{allPaths=**} {
      allow read: if true; // أو request.auth != null إذا أردت حصر ا��قراءة
      allow write: if request.auth != null;
    }
  }
}